## EVM-from-scratch

A simple implementation of EVM-from-scratch in RUST using the test cases given by [w1nt3r-eth](!https://github.com/w1nt3r-eth/evm-from-scratch).

---
# Run locally
Clone repository and then run the following commands:

` cd rust && cargo run `